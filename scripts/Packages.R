if(!require(CCA)){install.packages("CCA")} # Canonical correlation analysis
if(!require(CCP)){install.packages("CCP")} # Significance Tests for Canonical Correlation Analysis
if(!require(knitr)){install.packages("knitr")} # Report generation
if(!require(kableExtra)){install.packages("kableExtra")} # Gerar tabelas customizadas
# library(vegan)
# library(haven)
if(!require(readr)){install.packages("readr")} # Ler csv
if(!require(RColorBrewer)){install.packages("RColorBrewer")} # paletas legais
if(!require(dplyr)){install.packages("dplyr")} # Manipulação de dados
if(!require(foreach)){install.packages("foreach")} # Loops %do%%
if(!require(doParallel)){install.packages("doParallel")} # Loops %dopar%
if(!require(tidyr)){install.packages("tidyr")} # Tidy Messy Data management
if(!require(formattable)){install.packages("formattable")} # Tabelas
# if(!require(flextable)){install.packages("flextable")} # Tabelas
if(!require(DT)){install.packages("DT")} # Tabelas
if(!require(ggplot2)){install.packages("ggplot2")} # Gráficos
if(!require(viridis)){install.packages("viridis")} # Viridis Color Palettes
if(!require(htmlwidgets)){install.packages("htmlwidgets")} # Para salvar a wordcloud
if(!require(webshot)){install.packages("webshot")} # ajuda a salvar
if(!require(stringr)){install.packages("stringr")} # Manipulação de strings